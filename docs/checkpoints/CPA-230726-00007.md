CPA-230726-00007

Restoration Context

Project: DAN Platform.

Development principles:

Architecture first.

Brick-by-brick.

Code only after approval.

Preserve existing project style.

Full files instead of patches.

Current migration:

Event-> EventBus-> EventDispatcher-> Adapter (not implemented)-> MessageBus-> MessageDispatcher

Accepted decisions:

module_id.hpp is the permanent module registry.

message_id.hpp is the protocol registry.

ModuleId uses explicit protocol values.

MessageId currently aliases EventType values via constexpr/static_cast.

EventType remains the temporary source of truth.

Broadcast values intentionally differ:EventTarget::Broadcast = 0Module::Broadcast = 0xFF

Namespace MessageIds is used because Message type already exists.

Project style:#include <stdint.h>uint8_t / uint16_t

Current state:

module_id.hpp created.

message_id.hpp created.

Project builds successfully.

Next task: design Adapter only, no implementation before architectural approval.