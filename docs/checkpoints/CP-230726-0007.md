CP-230726-0007

DAN Platform — Checkpoint

Выполнено

Созданы message/module_id.hpp.

Созданы message/message_id.hpp.

Реестр модулей вынесен отдельно.

Реестр сообщений вынесен отдельно.

MessageId временно определяется через EventType, чтобы избежать дублирования числовых идентификаторов.

Принято использовать стиль проекта:

#include <stdint.h>

uint8_t, uint16_t

Проект успешно собирается без ошибок.

Архитектура

Event
  ↓
EventBus
  ↓
EventDispatcher
  ↓
Adapter (следующий этап)
  ↓
MessageBus
  ↓
MessageDispatcher

Следующий кирпичик

Спроектировать Adapter, затем только после утверждения реализовать его.